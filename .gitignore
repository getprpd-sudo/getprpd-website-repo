# Raw originals — never deploy these (large files, not web-served)
_raw_originals/

# 360 spin assets — section is hidden until reshoot, not needed on live site
assets/360/

# 3D model — not referenced anywhere on live site
assets/models/

# Nested git repo clone (accident — real repo is this folder)
GetPRPD Website/

# Dev-only files — no need to deploy these
dish-preview.html
360-shoot-guide.html
apps-script.gs
GOOGLE_SHEETS_SETUP.md

# OS / editor clutter
.DS_Store
Thumbs.db
*.swp
.claude/

.vercel

# Server credentials - never commit or deploy
*-website-*.json
service-account*.json
*API*KEY*.txt
operations/.planner-key
operations/private-data/
.env*.local
node_modules/
__pycache__/
*.py[cod]
.vercel/

# Rendered source-review files and other disposable working output
tmp/
.tmp/
output/

# Customer-specific operational records stay local because this repository is public
operations/BATCH_*_PACKING_MANIFEST_*.md
operations/WEEKLY_CLOSEOUT_20??-??-??.md

.env*
.vercel
